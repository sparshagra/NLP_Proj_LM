# Model Weights

Upload the trained model weights here directly via GitHub UI.

| File | Description |
|------|-------------|
| `phase1_model_weights.pt` | Phase 1 trained weights |
| `phase2_model_weights.pt` | Phase 2 trained weights |
| `phase3_model_weights.pt` | Phase 3 trained weights |

> **Note:** `.pt` files are excluded from git tracking. Upload them manually using the GitHub web interface ("Add file → Upload files").
